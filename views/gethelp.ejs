<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<title><%- title %></title>
	<link rel="stylesheet" type="text/css" href="/css/style.css">
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
	<script src="//code.jquery.com/jquery-1.11.0.min.js"></script>
	<script src="/socket.io/socket.io.js"></script>
	<script>
		$(function() { 
			var host = '<%- config.host %>';
			var socket = io.connect(host + '/roomlist');
			socket.on('connect', function(){
				console.log('Connection Established!');
			})
			socket.on('roomupdate', function(data){
				var procData = JSON.parse(data);
				$('.roomlist').html('');
				for (var i = 0; i < procData.length; i++) {
					var str = '<a href="gethelp/room/' + procData[i].room_number + '"><li>' + procData[i].room_name +'</li></a>';
					$('.roomlist').prepend(str);
				}
			})
			$(document).on('click', '#create', function() {
				var room_name = $('.newRoom').val();
				if(room_name != ''){
					var room_number = parseInt(Math.random() * 10000);
					socket.emit('newroom', {room_name:room_name, room_number:room_number});
					$('.newRoom').val('');
				}
			})
		})
	</script>
</head>
<body>
		<div style="position: fixed; top: 10px; left: 10px;"><a href=""><i class="fa fa-github" style="font-size: 3em;"></i></a></div>
		<div style="position: fixed; top: 10px; right: 10px;"><a href="">Give your Suggestions</a></div>

	<div id="platform">
		<p>
			In order to get help in any topic/issue. Type in the issue, it will be broadcasted to all the users on the website.<br>PS: Feature of bradcasting in particular group/circles will be added soon.
		</p>
			
			<p>
			<div class="cr-roomlist">
				<ul class="roomlist">
				</ul>
			</div>
			</p>
		</div>

	<div class="container">
		<h2 class="title">Steno</h2>
		<div class="userBox">
			<div><img src="<%- user.profilePic %>" class="userPic"></div>
			<div><h3 class="userName"><%- user.fullname %></h3>
			<h3 class="logout"> <a href="/logout">Logout</a></h3></div>
		</div>
		<div class="underline"></div>
		
		<div id="navigation">
			<a href="/user/todo"><button>Your to-do list</button></a>
			<a href="/user/gethelp"><button>Get Help</button></a>			
		</div>
	</div>
			<div class="newroom" >
				<input id="ss" type="text" class="newRoom" autocomplete="off">
				<button id="create">Get Help</button>
			</div>


</body>
</html>